# Home Assistant Configuration

Personal Home Assistant configuration:
- YAML-first setup
- Extensive use of packages
- Custom components
- Git-tracked configuration (runtime data excluded)

This repository contains only declarative configuration.
Secrets, runtime state, databases and media files are intentionally excluded.
